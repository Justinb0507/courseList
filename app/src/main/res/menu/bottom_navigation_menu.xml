<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

<item android:id="@+id/home_page"
    android:icon="@drawable/menu_selected"
    tools:ignore="MenuTitle"
    />

    <item android:id="@+id/cookies"
        android:icon="@drawable/cookies_selected"
        tools:ignore="MenuTitle" />

    <item android:id="@+id/calendar"
        android:icon="@drawable/calendar_selected"
        tools:ignore="MenuTitle"
        />

    <item android:id="@+id/course"
        android:icon="@drawable/course_selected"
        tools:ignore="MenuTitle" />

</menu>